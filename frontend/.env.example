# API Configuration
# Note: If these are not set, the app will automatically use the current hostname
# This allows it to work on localhost, network IP, and mobile devices automatically
# Only set these if you need to override the automatic detection

# VITE_API_BASE_URL=http://localhost:5001/api
# VITE_SOCKET_URL=http://localhost:5001

# For mobile/network access, the app will automatically detect the hostname
# Example: If accessed via http://192.168.1.13:3000, 
# it will use http://192.168.1.13:5001 for the backend